# Local development can omit DATABASE_URL and will use SQLite.
# Railway: connect a PostgreSQL service and expose its DATABASE_URL to this service.
DATABASE_URL=postgresql://user:password@host:5432/database

# Required when DATABASE_URL is set (production).
ADMIN_USER=admin
ADMIN_PASS=change-this-to-a-long-random-password
